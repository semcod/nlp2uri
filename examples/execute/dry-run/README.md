# Dry-run OS actions

Resolves natural language to URIs, compiles `OSAction` commands, and prints argv
without executing GUI tools.

Safe for CI and Docker (no desktop session required).

## Run

```bash
./e2e.sh
```
