# MCP tool handoff

Shows JSON payloads for MCP tools:

- `resolve_desktop_action` — `text/uri-list` + structured plan
- `execute_desktop_uri` — companion tool (see `nlp2uri.mcp`)

## Run

```bash
./e2e.sh
```
