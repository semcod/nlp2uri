syntax = "proto3";

package nlp2uri.cqrs.v1.desktop_window;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (desktop-window://{action})
// IR upstream: n/a
message DesktopWindowAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
