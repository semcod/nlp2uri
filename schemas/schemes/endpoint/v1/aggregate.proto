syntax = "proto3";

package nlp2uri.cqrs.v1.endpoint;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (endpoint://{proto}/{host}/{port}/{path})
// IR upstream: n/a
message EndpointAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
