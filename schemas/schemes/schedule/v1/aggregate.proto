syntax = "proto3";

package nlp2uri.cqrs.v1.schedule;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (schedule://{id})
// IR upstream: ScheduleSpecIR
message ScheduleAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
