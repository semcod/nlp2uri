syntax = "proto3";

package nlp2uri.cqrs.v1.ide_chat;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (ide-chat://{ide}/{action})
// IR upstream: n/a
message IdeChatAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
