syntax = "proto3";

package nlp2uri.cqrs.v1.device;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (device://{bus}/{id})
// IR upstream: n/a
message DeviceAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
