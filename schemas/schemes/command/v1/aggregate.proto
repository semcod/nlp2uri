syntax = "proto3";

package nlp2uri.cqrs.v1.command;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (command://{runtime_id}/{name})
// IR upstream: CommandSchemaIR
message CommandAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
