syntax = "proto3";

package nlp2uri.cqrs.v1.app;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (app://{app}/{action})
// IR upstream: n/a
message AppAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
