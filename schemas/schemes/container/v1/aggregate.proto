syntax = "proto3";

package nlp2uri.cqrs.v1.container;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (container://{runtime}/{name})
// IR upstream: n/a
message ContainerAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
