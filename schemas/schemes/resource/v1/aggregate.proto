syntax = "proto3";

package nlp2uri.cqrs.v1.resource;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (resource://{connector}/{id})
// IR upstream: ResourceSpecIR
message ResourceAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
