syntax = "proto3";

package nlp2uri.cqrs.v1.ide_command;

import "common/v1/uri.proto";

// Aggregate root: aggregate_id = uri.raw (ide-command://{ide}/{action})
// IR upstream: n/a
message IdeCommandAggregate {
  nlp2uri.cqrs.v1.common.Uri uri = 1;
  int64 version = 2;
  string state = 3;
}
